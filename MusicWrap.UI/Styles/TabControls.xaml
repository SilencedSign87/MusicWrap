<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- TabControl Style -->
    <Style x:Key="CustomTabControl" TargetType="TabControl">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabControl">
                    <Grid KeyboardNavigation.TabNavigation="Local">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <Border Grid.Row="0"
                                Background="Transparent"
                                Padding="0"
                                Margin="0,0,0,8"
                                ClipToBounds="True">
                            <StackPanel x:Name="HeaderPanel"
                                        IsItemsHost="True"
                                        Orientation="Horizontal"
                                        Background="Transparent"
                                        Margin="0"
                                        HorizontalAlignment="Left"/>
                        </Border>

                        <ContentPresenter x:Name="PART_SelectedContentHost"
                                          Grid.Row="1"
                                          Margin="0"
                                          ContentSource="SelectedContent"
                                          SnapsToDevicePixels="True"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="TabStripPlacement" Value="Bottom">
                            <Setter TargetName="HeaderPanel" Property="Grid.Row" Value="1"/>
                            <Setter TargetName="PART_SelectedContentHost" Property="Grid.Row" Value="0"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- TabItem Style -->
    <Style x:Key="CustomTabItem" TargetType="TabItem">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="8,6"/>
        <Setter Property="Margin" Value="2,0"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabItem">
                    <Border x:Name="Root"
                            Background="Transparent"
                            CornerRadius="4"
                            SnapsToDevicePixels="True"
                            UseLayoutRounding="True">
                        <Grid ClipToBounds="True">
                            <Border x:Name="SelectedBackground"
                                    Background="{DynamicResource AccentBrush100}"
                                    CornerRadius="4"
                                    Opacity="0"/>

                            <Border x:Name="HoverBackground"
                                    Background="{DynamicResource BackgroundLayerAlt}"
                                    CornerRadius="4"
                                    Opacity="0"/>

                            <Border x:Name="SelectionIndicator"
                                    Width="3"
                                    Height="16"
                                    CornerRadius="2"
                                    Background="{DynamicResource AccentBrush100}"
                                    Opacity="0"
                                    HorizontalAlignment="Left"
                                    VerticalAlignment="Center"/>

                            <ContentPresenter x:Name="ContentSite"
                                              ContentSource="Header"
                                              Margin="8,6,8,6"
                                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                              RecognizesAccessKey="True"/>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="HoverBackground" Property="Opacity" Value="1"/>
                        </Trigger>

                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="SelectedBackground" Property="Opacity" Value="0.18"/>
                            <Setter TargetName="HoverBackground" Property="Opacity" Value="0"/>
                            <Setter TargetName="SelectionIndicator" Property="Opacity" Value="1"/>
                            <Setter Property="Foreground" Value="{DynamicResource AccentBrush100}"/>
                        </Trigger>

                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsSelected" Value="True"/>
                                <Condition Property="IsMouseOver" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="SelectedBackground" Property="Opacity" Value="0.1"/>
                        </MultiTrigger>

                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="{DynamicResource TextDisabledBrush}"/>
                            <Setter TargetName="Root" Property="Opacity" Value="0.65"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- TabControl with vertical orientation -->
    <Style x:Key="VerticalTabControl" TargetType="TabControl" BasedOn="{StaticResource CustomTabControl}">
        <Setter Property="TabStripPlacement" Value="Left"/>
    </Style>

    
    
    <!-- Subtle style -->
    <Style x:Key="SubtleTabItem" TargetType="TabItem" BasedOn="{StaticResource CustomTabItem}">
        <Setter Property="Padding" Value="6,4"/>
        <Setter Property="Margin" Value="1,0"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="VerticalContentAlignment" Value="Stretch"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabItem">
                    <Border x:Name="Root"
                        Background="Transparent"
                        CornerRadius="4,4,0,0"
                        SnapsToDevicePixels="True"
                        UseLayoutRounding="True">
                        <Grid ClipToBounds="True">
                            <Border x:Name="SelectedBackground"
                                Background="{DynamicResource BackgroundControlHover}"
                                CornerRadius="4,4,0,0"
                                Opacity="0"/>

                            <Border x:Name="HoverBackground"
                                Background="{DynamicResource BackgroundControlPressed}"
                                CornerRadius="4,4,0,0"
                                Opacity="0"/>

                            <ContentPresenter ContentSource="Header"
                                          Margin="6,4,6,4"
                                          HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          RecognizesAccessKey="True"/>
                        </Grid>
                    </Border>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="HoverBackground" Property="Opacity" Value="1"/>
                        </Trigger>

                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="SelectedBackground" Property="Opacity" Value="1"/>
                            <Setter TargetName="HoverBackground" Property="Opacity" Value="0"/>
                        </Trigger>

                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="{DynamicResource TextDisabledBrush}"/>
                            <Setter TargetName="Root" Property="Opacity" Value="0.65"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="SubtleTabControl" TargetType="TabControl" BasedOn="{StaticResource CustomTabControl}">
        <Setter Property="Padding" Value="0"/>
        <Setter Property="BorderThickness" Value="0"/>
    </Style>

    <Style x:Key="SubtleVerticalTabControl" TargetType="TabControl" BasedOn="{StaticResource SubtleTabControl}">
        <Setter Property="TabStripPlacement" Value="Left"/>
    </Style>

</ResourceDictionary>
