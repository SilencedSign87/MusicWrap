<Window x:Class="MusicWrap.UI.Windows.LibrarySearcherWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:MusicWrap.UI.Controls"
        xmlns:local="clr-namespace:MusicWrap.UI.Windows"
        mc:Ignorable="d"
        Title="Library explorer" 
        Icon="/Resources/icon.ico"
        Height="700" Width="500"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Border Grid.Row="0" Padding="8,2">
            <Grid>
                <controls:CommandPaletteInput Width="250" VerticalAlignment="Center" HorizontalAlignment="Right"/>
            </Grid>
        </Border>

    </Grid>
</Window>
