<UserControl x:Class="MusicWrap.UI.Controls.AutocompleteMetadataInput"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:MusicWrap.UI.Controls"
             x:Name="Root"
             mc:Ignorable="d"
             Height="32"
             d:Width="200"
             IsVisibleChanged="UserControl_IsVisibleChanged"
             Unloaded="UserControl_Unloaded"
             ClipToBounds="False">
    <Grid>
        <local:CompactInputField x:Name="CompactInput"
                                 Text="{Binding Text, ElementName=Root, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                 PlaceholderText="{Binding PlaceholderText, ElementName=Root}"
                                 BeforeContent="{Binding BeforeContent, ElementName=Root}"
                                 AfterContent="{Binding AfterContent, ElementName=Root}"/>

        <Popup x:Name="SuggestionsPopup"
               IsOpen="{Binding IsOpen, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay}"
               StaysOpen="False"
               AllowsTransparency="True"
               Placement="Bottom"
               PlacementTarget="{Binding ElementName=CompactInput}"
               Width="300"
               MaxHeight="250">
            <Border Background="{StaticResource BackgroundFloating}"
                    BorderBrush="{StaticResource StrokeFloating}"
                    BorderThickness="1"
                    CornerRadius="4">
                <ListBox x:Name="SuggestionsListBox"
                         ItemsSource="{Binding Suggestions, RelativeSource={RelativeSource AncestorType=UserControl}}"
                         SelectedIndex="-1"
                         MouseDoubleClick="SuggestionsListBox_MouseDoubleClick"
                         ScrollViewer.VerticalScrollBarVisibility="Auto"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                         Padding="0"
                         BorderThickness="0">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}"
                                       Padding="8,6,8,6"
                                       TextTrimming="CharacterEllipsis"/>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Padding" Value="0"/>
                            <Setter Property="Margin" Value="0"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#F0F0F0"/>
                                </Trigger>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="#F0F0F0"/>
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsSelected" Value="True"/>
                                        <Condition Property="Selector.IsSelectionActive" Value="False"/>
                                    </MultiTrigger.Conditions>
                                    <Setter Property="Background" Value="#F0F0F0"/>
                                </MultiTrigger>
                            </Style.Triggers>
                        </Style>
                    </ListBox.ItemContainerStyle>
                </ListBox>
            </Border>
        </Popup>
    </Grid>
</UserControl>


