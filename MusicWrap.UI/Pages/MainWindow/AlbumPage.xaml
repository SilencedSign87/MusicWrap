<UserControl x:Class="MusicWrap.UI.Pages.MainWindow.AlbumPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MusicWrap.UI.Pages.MainWindow"
             xmlns:ex="clr-namespace:MusicWrap.UI.Helpers"
             xmlns:vm="clr-namespace:MusicWrap.UI.ViewModels.Library"
             xmlns:converters="clr-namespace:MusicWrap.UI.Converters"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=vm:LibraryViewModel+AlbumData}"
             d:DesignHeight="200" d:DesignWidth="200"
             Cursor="Hand">
    <UserControl.Resources>
        <converters:PathToImageConverter x:Key="PathToImageConverter"/>
    </UserControl.Resources>
    <!-- Renders an album -->
    <Border Background="Transparent" 
            BorderThickness="0"
            CornerRadius="4"
            Margin="4"
            Padding="0"
            >
        <Border.ContextMenu>
            <ContextMenu Placement="Right">
                <MenuItem Header="Play album" Click="PlayAlbum" Icon="{ex:Icon Glyph=E768}"/>
                <MenuItem Header="Play album next" Click="AddAlbumToNext" Icon="{ex:Icon Glyph=ECC8}"/>
                <MenuItem Header="Add to queue" Click="AddToQueue" Icon="{ex:Icon Glyph=E90B}"/>
                <Separator/>
                <MenuItem Header="Add to playlist" x:Name="ListSelectorMenuItem" Icon="{ex:Icon Glyph=ED0E}"/>
                <Separator/>
                <MenuItem Header="Edit album" Icon="{ex:Icon Glyph=E70F}"/>
                <MenuItem Header="Properties" Icon="{ex:Icon Glyph=E946}"/>
            </ContextMenu>
        </Border.ContextMenu>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <!-- Album Cover -->
            <Image Grid.Row="0"
                   Source="{Binding CoverImage}"
                   Stretch="UniformToFill"
                   VerticalAlignment="Top"
                   Width="150"
                   Height="150"
                   HorizontalAlignment="Center"
                   RenderOptions.BitmapScalingMode="LowQuality"
                   />
            
            <!-- Album Info -->
            <Border Grid.Row="1" 
                    Padding="8,4"
                    Background="Transparent">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0"
                               Text="{Binding Title}" 
                               FontSize="12" 
                               FontWeight="SemiBold"
                               Foreground="White"
                               TextTrimming="CharacterEllipsis"
                               TextWrapping="NoWrap"/>
                    <TextBlock Grid.Row="1"
                               FontSize="11" 
                               Foreground="#999999"
                               TextTrimming="CharacterEllipsis"
                               TextWrapping="NoWrap">
                        <Run Text="{Binding ArtistNames}"/>
                        <Run Text=" • "/>
                        <Run Text="{Binding Year}"/>
                    </TextBlock>
                </Grid>
            </Border>
        </Grid>
    </Border>
</UserControl>
