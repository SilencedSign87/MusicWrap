<UserControl x:Class="MusicWrap.UI.Pages.MainWindow.TrackInformationPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MusicWrap.UI.Pages.MainWindow"
             xmlns:vm="clr-namespace:MusicWrap.UI.ViewModels"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=vm:PlayerViewModel}"
             d:DesignHeight="450" d:DesignWidth="300">
    <ScrollViewer
        HorizontalScrollBarVisibility="Disabled"
        VerticalScrollBarVisibility="Auto"
        >
        <ScrollViewer.ContextMenu>
            <ContextMenu>
                <MenuItem Header="Edit information">
                    <MenuItem.Icon>
                        <TextBlock FontFamily="Segoe Fluent Icons" Text="&#xE70F;" FontSize="14"/>
                    </MenuItem.Icon>
                </MenuItem>
                <MenuItem Header="Open artwork" Click="OpenArtwork">
                    <MenuItem.Icon>
                        <TextBlock FontFamily="Segoe Fluent Icons" Text="&#xe8a7;" FontSize="14"/>
                    </MenuItem.Icon>
                </MenuItem>
            </ContextMenu>
        </ScrollViewer.ContextMenu>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition />
            </Grid.RowDefinitions>
            <!-- Information -->
            <StackPanel Orientation="Vertical" Grid.Row="0" Margin="0,0,0,16">
                <TextBlock Text="{Binding CurrentTrackTitle}" FontSize="14" Margin="0,0,0,8" TextTrimming="WordEllipsis" />
                <TextBlock Text="{Binding CurrentTrackAlbum}" FontSize="11" Opacity="0.75" TextTrimming="WordEllipsis"/>
                <TextBlock Text="{Binding CurrentTrackArtists}" FontSize="11" Opacity="0.75" TextTrimming="WordEllipsis"/>
                <TextBlock Text="{Binding CurrentTrackYear}" FontSize="11" Opacity="0.75" />
                <TextBlock Text="{Binding CurrentTrackChannels}" FontSize="11" Opacity="0.75" />

                <TextBlock FontSize="11" Opacity="0.75" TextTrimming="WordEllipsis">
                    <Run Text="{Binding CurrentTrackFormat}" />
                    <Run Text=" • "/>
                    <Run Text="{Binding CurrentTrackBitDepth}" />
                    <Run Text=" • "/>
                    <Run Text="{Binding CurrentTrackSampleRate}" />
                    <Run Text=" • "/>
                    <Run Text="{Binding CurrentTrackBitrate}" />
                    <Run Text=" • "/>
                    <Run Text="{Binding FormattedDuration}" />
                </TextBlock>

            </StackPanel>
            <!-- Artwork -->
            <Grid Grid.Row="1">

                <Image
                    HorizontalAlignment="Stretch"
                    Source="{Binding CurrentTrackImage}"
                    RenderOptions.BitmapScalingMode="HighQuality"
                    Stretch="UniformToFill"
                    />
            </Grid>

        </Grid>
    </ScrollViewer>
</UserControl>
