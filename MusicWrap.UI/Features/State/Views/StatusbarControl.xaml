<UserControl x:Class="MusicWrap.UI.Features.State.StatusbarControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MusicWrap.UI.Features.State"
             xmlns:vm="clr-namespace:MusicWrap.UI.Features.State.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="24" d:DesignWidth="800"
             BorderThickness="0,1,0,0"
             d:DataContext="{d:DesignInstance Type=vm:StatusbarViewModel}"
             BorderBrush="{StaticResource StrokeSurface}"
             >
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition  Width="Auto"/>
            <ColumnDefinition  Width="*"/>
            <ColumnDefinition  Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel x:Name="LeftPanel" Grid.Column="0" Grid.Row="0"  Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="4,0,2,0">
            <TextBlock Text="{Binding Left.Icon}" FontFamily="Segoe Fluent Icons" Margin="0,0,4,0"/>
            <TextBlock Text="{Binding Left.Text}"/>
            <ItemsControl ItemsSource="{Binding Left.Actions}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button 
                            Style="{StaticResource SubtleButton}"
                            Command="{Binding Command}"
                            CommandParameter="{Binding CommandParameter}"
                            Margin="0,0,4,0"
                            >
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding Icon}" FontFamily="Segoe Fluent Icons" />
                                <TextBlock Text="{Binding Text}" />
                            </StackPanel>
                        </Button>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>

        <StackPanel x:Name="CenterPanel" Grid.Column="1" Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="2,0">
            <TextBlock Text="{Binding Center.Icon}" FontFamily="Segoe Fluent Icons" Margin="0,0,4,0"/>
            <TextBlock Text="{Binding Center.Text}"/>
            <ItemsControl ItemsSource="{Binding Center.Actions}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button 
                Style="{StaticResource SubtleButton}"
                Command="{Binding Command}"
                CommandParameter="{Binding CommandParameter}"
                Margin="0,0,4,0"
                >
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding Icon}" FontFamily="Segoe Fluent Icons" />
                                <TextBlock Text="{Binding Text}" />
                            </StackPanel>
                        </Button>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>

        <StackPanel x:Name="RightPanel" Grid.Column="2" Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="2,0,4,0">
            <TextBlock Text="{Binding Right.Icon}" FontFamily="Segoe Fluent Icons" Margin="0,0,4,0"/>
            <TextBlock Text="{Binding Right.Text}"/>
            <ItemsControl ItemsSource="{Binding Right.Actions}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button 
                Style="{StaticResource SubtleButton}"
                Command="{Binding Command}"
                CommandParameter="{Binding CommandParameter}"
                Margin="0,0,4,0"
                >
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding Icon}" FontFamily="Segoe Fluent Icons" />
                                <TextBlock Text="{Binding Text}" />
                            </StackPanel>
                        </Button>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
        
        <ProgressBar Grid.Column="0"
                     Grid.Row="0"
                     Grid.ColumnSpan="3"
                     VerticalAlignment="Bottom"
                     Minimum="0"
                     Maximum="{Binding ProgressMaximum}"
                     Value="{Binding ProgressValue}"
                     Visibility="{Binding IsVisible, Converter={StaticResource BooleanToVisibilityConverter}}"
                     IsIndeterminate="{Binding IsIndeterminate}"
                     Foreground="{StaticResource AccentBrush80}"
                     Background="Transparent"/>
    </Grid>
</UserControl>
